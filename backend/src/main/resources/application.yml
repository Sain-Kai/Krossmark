spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/krossmark
    username: krossmark
    password: krossmark
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080